import 'package:flutter/material.dart';

class Fertilizers extends StatelessWidget {
  const Fertilizers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Fertilizers list"),
    );
  }
}
