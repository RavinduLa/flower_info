import 'package:flutter/material.dart';

class DiseaseRecently extends StatelessWidget {
  const DiseaseRecently({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
