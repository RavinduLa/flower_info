import 'package:flutter/material.dart';

class FLowers extends StatelessWidget {
  const FLowers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Flowers list"),
    );
  }
}
